(()=>{
'use strict';
const VERSION='20260730-SHEET-PROFILE-LOGIN-AUTHORITY-V1';
const KEY='herohealth_learning_platform_rc2';
const ENDPOINT='https://script.google.com/macros/s/AKfycbxU82Rg4KFStuZToOGlyX-rgzVkLpZ7yO1tW-gzui782eR7akes_HNZ5ec2TDUDh8J1/exec';
const clean=v=>String(v??'').trim().replace(/\s+/g,'');
const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch(_){return{}}};
const write=v=>localStorage.setItem(KEY,JSON.stringify(v));
function toast(text){let n=document.createElement('div');n.className='toast';n.textContent=text;document.body.appendChild(n);setTimeout(()=>n.remove(),6000)}
function busy(on,text){let box=document.getElementById('hh-sheet-profile-login');if(on&&!box){box=document.createElement('div');box.id='hh-sheet-profile-login';box.style.cssText='position:fixed;inset:0;z-index:2147483647;background:rgba(15,23,42,.9);display:grid;place-items:center;padding:24px;color:#fff;font:800 18px system-ui;text-align:center;line-height:1.6;white-space:pre-line';document.body.appendChild(box)}if(box){box.textContent=text||'กำลังตรวจสอบรหัสจาก Google Sheet…';if(!on)box.remove()}}
function jsonp(studentId){return new Promise((resolve,reject)=>{const cb='HHLOGIN_'+Date.now()+'_'+Math.random().toString(36).slice(2),s=document.createElement('script');let done=false;const finish=(err,data)=>{if(done)return;done=true;clearTimeout(timer);try{delete window[cb]}catch(_){};s.remove();err?reject(err):resolve(data)};const timer=setTimeout(()=>finish(new Error('sheet_timeout')),30000);window[cb]=data=>finish(null,data);s.onerror=()=>finish(new Error('sheet_load_failed'));const q=new URLSearchParams({action:'student',studentId,callback:cb,_:String(Date.now()),loginAuthority:'sheet-v1'});s.src=ENDPOINT+'?'+q.toString();s.async=true;(document.body||document.head).appendChild(s)})}
function validProfile(api,sid){const p=api?.authoritativeState?.profile||api?.profile||null;if(!api?.ok||api?.found!==true||!p)return null;const id=clean(p.studentId||api.studentId);if(id!==sid)return null;const group=String(p.group||api?.authoritativeState?.group||'').trim().toUpperCase();if(!p.fullName||!p.section||!group)return null;return{studentId:id,fullName:String(p.fullName).trim(),section:String(p.section).trim(),group,nickname:String(p.nickname||'').trim(),active:true,sheetAuthority:true,source:'HH_Profiles'} }
async function lookup(form){const sid=clean(new FormData(form).get('studentId'));if(!sid){toast('กรุณากรอกรหัสนักเรียน');return}busy(true,'กำลังตรวจสอบรหัส '+sid+'\nจาก HH_Profiles ใน Google Sheet');try{const api=await jsonp(sid),profile=validProfile(api,sid);if(!profile){busy(false);toast('ไม่พบรหัสนักเรียนที่ใช้งานได้ใน HH_Profiles กรุณาติดต่อครู');return}const state=read();state.pendingProfile=profile;state.profile=null;state.view='student';state.sheetLoginAuthority=VERSION;state.sheetLoginVerifiedAt=new Date().toISOString();write(state);localStorage.removeItem('herohealth_active_student_id');busy(false);location.reload()}catch(err){busy(false);console.error('[Sheet Profile Login]',err);toast('เชื่อม Google Sheet ไม่สำเร็จ กรุณาลองใหม่อีกครั้ง')}}
function install(){if(!window.HH){setTimeout(install,30);return}window.HH.lookup=lookup;window.HH.__sheetProfileLoginAuthority=VERSION;document.documentElement.dataset.hhSheetProfileLogin='V1';console.info('[HeroHealth] Sheet profile login authority installed',VERSION)}
install();
})();