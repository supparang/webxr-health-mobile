export const name='จาน 5 หมู่';
const groups=[{key:'grain',label:'ธัญพืช',icons:['🍞','🍚','🥖','🥨']},{key:'veg',label:'ผัก',icons:['🥦','🥕','🥒','🥬']},{key:'protein',label:'โปรตีน',icons:['🥩','🍗','🥚','🐟']},{key:'fruit',label:'ผลไม้',icons:['🍎','🍌','🍇','🍊']},{key:'dairy',label:'นม',icons:['🥛','🧀']}];
export function init(state,hud){ const pick=groups[Math.floor(Math.random()*groups.length)]||groups[3]; state.currentTarget=(pick&&pick.key)?pick.key:'fruit';
  const el=document.getElementById('targetWrap'); if(el) el.style.display='block'; const badge=document.getElementById('targetBadge'); if(badge){ const g=groups.find(x=>x.key===state.currentTarget); badge.textContent=g?g.label:state.currentTarget; } }
export function pickMeta(){ const g=groups[Math.floor(Math.random()*groups.length)]||groups[3]; const char=(g.icons||['🍎'])[Math.floor(Math.random()*((g.icons||['🍎']).length))]; return {type:'groups',group:g.key,char}; }
export function onHit(meta,systems,state){ const ok=state.currentTarget&&meta.group===state.currentTarget; const delta=ok?7:-2; systems.score.add(delta); systems.fx.spawn3D(null,(delta>0?'+':'')+delta,delta>0?'good':'bad');
  if(ok){ systems.sfx.play('sfx-good'); state.ctx.targetHitsTotal=(state.ctx.targetHitsTotal||0)+1; if((state.ctx.targetHitsTotal%3)===0){ const all=['grain','veg','protein','fruit','dairy']; let next=state.currentTarget; while(next===state.currentTarget){ next=all[Math.floor(Math.random()*all.length)]; } state.currentTarget=next; const labels={grain:'ธัญพืช',veg:'ผัก',protein:'โปรตีน',fruit:'ผลไม้',dairy:'นม'}; const badge=document.getElementById('targetBadge'); if(badge) badge.textContent=labels[next]||next; } } else { if(!systems.power.consumeShield()){ systems.sfx.play('sfx-bad'); } } }