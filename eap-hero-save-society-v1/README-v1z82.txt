v1z82 Hard Storage Split

Use index.html and eap-hero.js. This release saves a capped persistence snapshot only, preventing localStorage quota errors. Full artifacts remain in active memory and should later be moved to server/IndexedDB.
