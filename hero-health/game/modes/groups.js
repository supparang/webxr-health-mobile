export const name='groups';
const groups=[{key:'grain',labelTH:'ธัญพืช',labelEN:'Grain',icons:['🍞','🍚','🥖','🥨']},{key:'veg',labelTH:'ผัก',labelEN:'Vegetable',icons:['🥦','🥕','🥒','🥬']},{key:'protein',labelTH:'โปรตีน',labelEN:'Protein',icons:['🥩','🍗','🥚','🐟']},{key:'fruit',labelTH:'ผลไม้',labelEN:'Fruit',icons:['🍎','🍌','🍇','🍊']},{key:'dairy',labelTH:'นม',labelEN:'Dairy',icons:['🥛','🧀']}];
export function init(state,hud,diff){ state.currentTarget=groups[Math.floor(Math.random()*groups.length)].key; const el=document.getElementById('targetWrap'); if(el) el.style.display='block'; const badge=document.getElementById('targetBadge'); if(badge){ const g=groups.find(x=>x.key===state.currentTarget); badge.textContent=g? (state.L==='TH'? g.labelTH : g.labelEN) : state.currentTarget; } }
export function pickMeta(diff,state){ const g=groups[Math.floor(Math.random()*groups.length)]; const char=g.icons[Math.floor(Math.random()*g.icons.length)]; return {type:'groups',group:g.key,char}; }
export function onHit(meta,systems,state){ const ok=state.currentTarget && meta.group===state.currentTarget; if(ok){ systems.score.add(7); state.ctx.targetHitsTotal=(state.ctx.targetHitsTotal||0)+1; if((state.ctx.targetHitsTotal % 3)===0){ const all=['grain','veg','protein','fruit','dairy']; let next=state.currentTarget; while(next===state.currentTarget){ next=all[Math.floor(Math.random()*all.length)]; } state.currentTarget=next; const badge=document.getElementById('targetBadge'); const m={grain:['ธัญพืช','Grain'],veg:['ผัก','Vegetable'],protein:['โปรตีน','Protein'],fruit:['ผลไม้','Fruit'],dairy:['นม','Dairy']}; if(badge) badge.textContent=(state.L==='TH'? m[next][0] : m[next][1]); } } else { systems.score.add(-2);} }