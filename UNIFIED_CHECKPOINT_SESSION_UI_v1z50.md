# UNIFIED_CHECKPOINT_SESSION_UI_v1z50

Fix:
- S3/S6 no longer look like emergency fallback pages.
- Checkpoint-adjacent sessions use a normal Session Path UI.
- They are still safe-routed to avoid the previous black screen.

S3/S6 remain normal sessions.
Boss Gates remain checkpoints after criteria are met.
