{
  "name": "herohealth-english-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "main.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "lint": "node --check main.js && node --check english-week-authority.js && node --check english-week-journey.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
