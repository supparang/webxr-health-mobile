/* CSAI2102 AI Quest — S6 Minimax Arena Result Sync v5.0.0 */
(()=>{
'use strict';
const VERSION='v5.0.0-s6-minimax-profile-recovery-retry',STATE_KEY='CSAI2102_AIQUEST_V16_M1_GOOGLE_SHEETS',PENDING_KEY='AIQUEST_S6_PENDING_V500',RECEIPT_KEY='AIQUEST_S6_RECEIPT_V500',FALLBACK_ENDPOINT='https://script.google.com/macros/s/AKfycbwXSUHbhVbZtKcjNIDzs4TawAohdeInm1MxLpomVeST2JilOL3L0LWQtT4_Yb7fbJG9/exec';let busy=false;
const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'null')}catch(_){return null}},write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v))}catch(_){}},num=v=>{const n=Number(v);return Number.isFinite(n)?n:0},str=v=>String(v==null?'':v),state=()=>read(STATE_KEY)||{};
function endpoint(){try{return window.AIQuestDataContract?.loadConfig?.().appsScriptUrl||FALLBACK_ENDPOINT}catch(_){return FALLBACK_ENDPOINT}}
function profile(){try{const p=window.AIQuestStorage?.getProfile?.()||{};if(p.studentId)return p}catch(_){}const st=state();return {studentId:st.studentId||st.profile?.studentId||'',studentName:st.studentName||st.profile?.studentName||st.name||'',section:st.section||st.profile?.section||'101'}}
function onS6Result(){const st=state(),h=str(document.getElementById('resultHeading')?.textContent).toLowerCase();return /minimax|alpha.?beta|s6|session 6/.test(h)||!!st.completed?.m6||num(st.bestScore?.m6)>0}
function build(){const st=state(),p=profile(),r=st.lastRun||st.lastResult||st.currentResult||{},score=num(document.getElementById('resultScore')?.textContent)||num(st.bestScore?.m6)||num(r.score),stars=(str(document.getElementById('resultStars')?.textContent).match(/★/g)||[]).length||num(st.stars?.m6),total=num(r.total||r.questionTotal||r.itemsTotal||st.s6Total),correct=num(r.correct||r.correctCount||r.itemsCorrect||st.s6Correct);return {attemptId:'s6_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,10),studentId:str(p.studentId),studentName:str(p.studentName||p.name),section:'101',sessionId:'s6',missionId:'m6',missionTitle:'Minimax Arena',difficulty:str(st.difficulty||'normal'),score,stars,mastered:!!st.mastered?.m6||score>=85,usedTimeSec:num(r.usedTimeSec||r.usedSec||st.bestTime?.m6),timeLeftSec:num(r.timeLeftSec),accuracy:total?Math.round(correct*100/total):'',correct,total,wrong:total?Math.max(0,total-correct):0,maxCombo:num(r.maxCombo),helpUsed:num(r.helpUsed),bossWin:false,reflection1:str(document.getElementById('ref1')?.value),reflection2:str(document.getElementById('ref2')?.value),reflection3:str(document.getElementById('ref3')?.value),clientTs:new Date().toISOString(),userAgent:navigator.userAgent,pageUrl:location.href,schemaVersion:VERSION,gateStatus:score>=85?'mastered':'passed',submitStatus:'queued',isPractice:false,isGraded:true,extraJson:{sessionTitle:'Game Tree / MAX–MIN / Utility / Alpha–Beta Pruning'}}}
function status(m){const b=document.getElementById('saveStatusBox');if(b)b.textContent='Save Status: '+m}
async function send(payload){if(!payload?.studentId)return false;await fetch(endpoint(),{method:'POST',mode:'no-cors',cache:'no-store',keepalive:true,headers:{'Content-Type':'text/plain;charset=UTF-8'},body:JSON.stringify({action:'sync_v23',kind:'attempt',payload})});return true}
async function flush(){if(busy)return false;const pending=read(PENDING_KEY);if(!pending?.payload)return false;busy=true;try{await send(pending.payload);write(RECEIPT_KEY,{attemptId:pending.payload.attemptId,status:'queued',at:new Date().toISOString(),version:VERSION});localStorage.removeItem(PENDING_KEY);status('S6 ส่งข้อมูลเข้า Teacher แล้ว');return true}catch(err){write(PENDING_KEY,{...pending,lastError:String(err),retryAt:new Date().toISOString()});status('S6 เก็บคิวไว้ กำลังลองส่งใหม่');return false}finally{busy=false}}
function queueCurrent(){if(!onS6Result())return false;const p=build();if(!p.studentId){status('ไม่พบ Student Profile: กรุณากลับเมนู ตรวจ Profile แล้วเปิด S6 ใหม่');return false}if(!p.score&&!p.stars&&!state().completed?.m6){status('ไม่พบผล S6 สำหรับส่ง');return false}write(PENDING_KEY,{payload:p,queuedAt:new Date().toISOString(),version:VERSION});status('กำลังส่งผล S6 เข้า Google Sheets…');flush();return true}
document.addEventListener('click',ev=>{if(ev.target?.closest?.('#btnSaveResult'))queueCurrent()},true);setInterval(flush,3500);window.addEventListener('online',flush);setTimeout(flush,900);window.AIQuestS6ResultSyncV500={queueCurrent,flush,profile};console.log('[AIQuest] '+VERSION+' loaded');
})();
