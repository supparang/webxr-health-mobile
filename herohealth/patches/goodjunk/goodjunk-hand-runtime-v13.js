import { FilesetResolver, HandLandmarker } from 'https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.14/vision_bundle.mjs';

const VERSION='goodjunk-mobile-easy-detect-hand-v13.1.0';
const WASM='https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.14/wasm';
const MODEL='https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task';
let model,initPromise,stream,video,work,ctx,running=false,busy=false,timer=0,cursor=null,velocity={x:0,y:0},lastRaw=null,lastRawAt=0,noHand=0,pausedUntil=0,lastGoodPoint=null,lastGoodAt=0;
const state={version:VERSION,workerReady:false,detected:false,delegate:'',inferenceMs:0,profile:'mobile-easy-detect',error:'',recoveries:0,inputFps:0,source:''};
function status(t,c='handLite'){const b=document.querySelector('.missionBox');b?.classList.remove('handLite','touchRescue','cameraLite');b?.classList.add(c);if(b)b.dataset.handStatus=t;const m=document.getElementById('loadMsg');if(m)m.textContent=t}
async function load(){if(model)return model;if(initPromise)return initPromise;initPromise=(async()=>{status('กำลังโหลด Mobile Hand AR v13.1…');const r=await FilesetResolver.forVisionTasks(WASM);const opt=d=>({baseOptions:{modelAssetPath:MODEL,delegate:d},runningMode:'VIDEO',numHands:1,minHandDetectionConfidence:.16,minHandPresenceConfidence:.16,minTrackingConfidence:.16});try{model=await HandLandmarker.createFromOptions(r,opt('GPU'));state.delegate='GPU'}catch(e){model=await HandLandmarker.createFromOptions(r,opt('CPU'));state.delegate='CPU'}state.workerReady=true;return model})().catch(e=>{state.error=String(e?.message||e);initPromise=null;throw e});return initPromise}
async function camera(target){if(stream?.active&&target.srcObject===stream)return;stream=await navigator.mediaDevices.getUserMedia({audio:false,video:{facingMode:'user',width:{ideal:640},height:{ideal:480},frameRate:{ideal:20,max:24}}});window.__GJ_CAMERA_STREAM__=stream;target.srcObject=stream;target.muted=true;target.playsInline=true;await target.play()}
function prep(){if(work)return;work=document.createElement('canvas');work.width=256;work.height=144;ctx=work.getContext('2d',{alpha:false,desynchronized:true})}
function setWorkSize(searching){const w=searching?256:192,h=searching?144:108;if(work.width!==w){work.width=w;work.height=h}}
function nextDelay(){if(!state.detected)return state.inferenceMs>600?360:220;if(state.inferenceMs>700)return 420;if(state.inferenceMs>350)return 260;if(state.inferenceMs>180)return 170;return 110}
function schedule(){clearTimeout(timer);if(!running)return;timer=setTimeout(()=>infer().finally(schedule),nextDelay())}
function project(raw,now){if(lastRaw){const dt=Math.max(16,now-lastRawAt);const vx=(raw.x-lastRaw.x)/dt,vy=(raw.y-lastRaw.y)/dt;velocity.x=velocity.x*.62+vx*.38;velocity.y=velocity.y*.62+vy*.38}else velocity={x:0,y:0};lastRaw=raw;lastRawAt=now;const lead=Math.min(65,Math.max(18,state.inferenceMs*.22));return{x:Math.max(0,Math.min(innerWidth,raw.x+velocity.x*lead)),y:Math.max(0,Math.min(innerHeight,raw.y+velocity.y*lead))}}
function smooth(p){if(!cursor)cursor={...p};const a=.76;cursor.x+=(p.x-cursor.x)*a;cursor.y+=(p.y-cursor.y)*a;return cursor}
function pointFromHand(h){const index=h?.[8];if(index&&index.x>=-.08&&index.x<=1.08&&index.y>=-.08&&index.y<=1.08)return{point:{x:(1-index.x)*innerWidth,y:index.y*innerHeight},source:'index'};const ids=[0,5,9,13,17],valid=ids.map(i=>h?.[i]).filter(Boolean);if(valid.length<3)return null;const palm=valid.reduce((a,p)=>({x:a.x+p.x,y:a.y+p.y}),{x:0,y:0});return{point:{x:(1-palm.x/valid.length)*innerWidth,y:(palm.y/valid.length)*innerHeight},source:'palm'}}
async function infer(){if(!running||busy||document.hidden||!model||!video||video.readyState<2)return;if(performance.now()<pausedUntil)return;busy=true;const t=performance.now();try{setWorkSize(!state.detected||noHand>0);ctx.drawImage(video,0,0,work.width,work.height);const r=model.detectForVideo(work,t);state.inferenceMs=Math.round(performance.now()-t);state.inputFps=Math.round(1000/Math.max(1,nextDelay()));if(state.inferenceMs>1800){pausedUntil=performance.now()+1600;state.recoveries++;status('Hand Tracking พักสั้น ๆ • เกมยังเล่นต่อได้','touchRescue');return}const h=r?.landmarks?.[0],resolved=h?pointFromHand(h):null;if(!resolved){state.detected=false;noHand++;if(lastGoodPoint&&performance.now()-lastGoodAt<850){window.__GJ_HAND_POINTS__=[lastGoodPoint];status('กำลังรักษาตำแหน่งมือ • ขยับช้า ๆ');return}window.__GJ_HAND_POINTS__=[];lastRaw=null;velocity={x:0,y:0};if(noHand>1)status('ถอยมือให้เห็นทั้งฝ่ามือ • ไม่ต้องชิดกล้องมาก');return}noHand=0;state.detected=true;state.source=resolved.source;const p=smooth(project(resolved.point,performance.now()));lastGoodPoint={x:p.x,y:p.y};lastGoodAt=performance.now();window.__GJ_HAND_POINTS__=[p];status(`ตรวจพบ${resolved.source==='index'?'นิ้ว':'ฝ่ามือ'} • ${state.delegate} ${state.inferenceMs}ms`);window.__GJ_SELECT_AT__?.(p.x,p.y,'mobile-v13.1');window.__GJ_HANDLANDMARKER_STATUS__={...state,cursor:p,velocity}}catch(e){state.error=String(e?.message||e);pausedUntil=performance.now()+1400;state.recoveries++;status('Hand AR สะดุด • เกมยังเล่นต่อและจะลองใหม่','touchRescue')}finally{busy=false}}
async function waitBridge(){const t=performance.now();while(performance.now()-t<7000){if(typeof window.__GJ_SELECT_AT__==='function')return;await new Promise(r=>setTimeout(r,40))}throw new Error('selection bridge unavailable')}
async function start(v){stop(false);video=v;prep();await Promise.all([load(),camera(v),waitBridge()]);running=true;pausedUntil=0;cursor=null;lastRaw=null;lastGoodPoint=null;noHand=0;velocity={x:0,y:0};window.__GJ_HAND_POINTS__=[];status(`Mobile Hand AR v13.1 พร้อม • ${state.delegate}`);schedule();return true}
function stop(close=true){running=false;clearTimeout(timer);busy=false;window.__GJ_HAND_POINTS__=[];cursor=null;lastRaw=null;lastGoodPoint=null;if(close&&stream){stream.getTracks().forEach(t=>t.stop());stream=null}}
window.GJMobileHandV7={VERSION,state,start,stop,preload:load,retryNow:async()=>{pausedUntil=0;running=true;schedule();return true}};
setTimeout(()=>load().catch(()=>{}),180);window.addEventListener('pagehide',()=>stop(true));