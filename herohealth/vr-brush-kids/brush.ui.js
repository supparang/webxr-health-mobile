function setText(node, value) {   if (!node) return;   node.textContent = String(value ?? ''); }  function clamp(v, a, b) {   return Math.max(a, Math.min(b, v)); }  function ensureArray(value) {   return Array.isArray(value) ? value : []; }  function findZoneButton(zoneId) {   return document.querySelector(`[data-zone="${zoneId}"]`); }  function zoneStateFromVm(zone) {   if (!zone) return 'idle';   if (zone.done) return 'done';   if (zone.active) return 'active';   if ((zone.cleanPercent || 0) > 0) return 'active';   return 'idle'; }  function zoneLabelText(zone) {   const clean = Math.round(zone?.cleanPercent || 0);   return `${zone?.label || '-'} ${clean}%`; }  function applyVisualStateToZoneButton(node, zone) {   if (!node) return;    const state = zoneStateFromVm(zone);   node.dataset.state = state;   node.setAttribute('aria-pressed', zone?.active ? 'true' : 'false');    if ((zone?.threatPercent || 0) >= 70 && !zone?.done) {     node.style.boxShadow = '0 0 0 3px rgba(255,120,145,.18), 0 8px 20px rgba(71,156,197,.10)';   } else {     node.style.boxShadow = '';   } }  function sceneLabel(sceneId) {   const labels = { launcher: 'Start', intro: 'Intro', scan: 'Scan', guided: 'Brush', pressure: 'Rush', fever: 'Fever', bossBreak: 'Break Shield', boss: 'Attack Boss', finish: 'Finish', summary: 'Summary' };   return labels[sceneId] || sceneId || '-'; }  function renderSummaryMetric(node, value, suffix = '') {   if (!node) return;   node.textContent = `${value ?? 0}${suffix}`; }  export function createBrushUI(el) {   function renderTopHud(vm = {}) {     setText(el.timeText, vm.timeText || '0s');     setText(el.scoreText, vm.scoreText ?? 0);     setText(el.comboText, vm.comboText ?? 0);     setText(el.threatText, vm.threatText || '0%');     setText(el.sceneText, sceneLabel(vm.sceneText));      if (el.threatText) {       const n = Number.parseInt(String(vm.threatText || '0%'), 10);       if (Number.isFinite(n)) {         if (n >= 75) el.threatText.style.color = '#c93d5d';         else if (n >= 45) el.threatText.style.color = '#9b7200';         else el.threatText.style.color = '';       }     }      if (el.comboText) {       const combo = Number(vm.comboText || 0);       if (combo >= 12) {         el.comboText.style.color = '#ff7e48';         el.comboText.style.textShadow = '0 0 10px rgba(255,126,72,.35)';       } else if (combo >= 6) {         el.comboText.style.color = '#4aaef0';         el.comboText.style.textShadow = '0 0 8px rgba(74,174,240,.28)';       } else {         el.comboText.style.color = '';         el.comboText.style.textShadow = '';       }     }      if (el.scoreText) {       if (vm.feverActive) {         el.scoreText.style.color = '#e5672d';         el.scoreText.style.textShadow = '0 0 12px rgba(255,160,88,.35)';       } else {         el.scoreText.style.color = '';         el.scoreText.style.textShadow = '';       }     }   }    function renderMiniMap(zones = []) {     const list = ensureArray(zones);      list.forEach((zone) => {       const node = findZoneButton(zone.id);       if (!node) return;       node.textContent = zoneLabelText(zone);       applyVisualStateToZoneButton(node, zone);       node.title = `${zone.label} • clean ${Math.round(zone.cleanPercent || 0)}% • threat ${Math.round(zone.threatPercent || 0)}%`;        const ring = document.querySelector(`[data-ring-zone="${zone.id}"]`);       if (!ring) return;        ring.classList.toggle('is-zone-active', !!zone.active);       ring.classList.toggle('is-zone-done', !!zone.done);     });   }    function renderCoach(face, line) {     setText(el.coachFace, face || '🪥');     setText(el.coachLine, line || '');   }    function renderScanHud(vm = {}) {     const active = !!(vm.timerText || vm.foundText);     el.scanCard?.classList.toggle('is-collapsed', !active);      setText(el.scanTimerText, vm.timerText || '');     setText(el.scanFoundText, vm.foundText || '');      if (el.scanTimerText) el.scanTimerText.style.opacity = vm.timerText ? '1' : '.35';     if (el.scanFoundText) el.scanFoundText.style.opacity = vm.foundText ? '1' : '.35';   }    function renderBossBreakHud(vm = {}) {     const active = !!(vm.timerText || vm.countText || vm.shieldText);     el.bossCard?.classList.toggle('is-collapsed', !active);      setText(el.bossShieldText, vm.shieldText || '');     setText(el.bossBreakTimerText, vm.timerText || '');     setText(el.bossBreakCountText, vm.countText || '');      if (el.bossBreakTimerText) {       const timerNum = Number.parseInt(String(vm.timerText || '').replace(/[^\d-]/g, ''), 10);       if (Number.isFinite(timerNum) && timerNum <= 3 && timerNum >= 0) {         el.bossBreakTimerText.style.color = '#c93d5d';         el.bossBreakTimerText.style.textShadow = '0 0 10px rgba(255,102,126,.32)';       } else {         el.bossBreakTimerText.style.color = '';         el.bossBreakTimerText.style.textShadow = '';       }     }   }    function renderSummary(result = {}) {     if (el.summaryModal) el.summaryModal.hidden = false;      renderSummaryMetric(el.summaryRank, result.finalRank || '-');     renderSummaryMetric(el.summaryScore, result.finalScore || 0);     renderSummaryMetric(el.summaryCoverage, result.coveragePercent || 0, '%');     renderSummaryMetric(el.summaryAccuracy, result.accuracyPercent || 0, '%');     setText(el.summaryAdvice, result.summaryAdvice || 'ลองเล่นอีกรอบเพื่อดูความต่างของผลลัพธ์');   }    function closeSummary() {     if (el.summaryModal) el.summaryModal.hidden = true;   }    function renderObjective(text, sceneId = '') {     if (!el.objectiveText) return;     setText(el.objectiveText, text || '');      el.objectiveCard?.classList.remove(       'is-scan-mode',       'is-guided-mode',       'is-pressure-mode',       'is-boss-mode',       'is-finish-mode'     );      if (sceneId === 'scan') {       el.objectiveCard?.classList.add('is-scan-mode');     } else if (sceneId === 'guided' || sceneId === 'intro' || sceneId === 'launcher') {       el.objectiveCard?.classList.add('is-guided-mode');     } else if (sceneId === 'pressure' || sceneId === 'fever') {       el.objectiveCard?.classList.add('is-pressure-mode');     } else if (sceneId === 'bossBreak' || sceneId === 'boss') {       el.objectiveCard?.classList.add('is-boss-mode');     } else if (sceneId === 'finish' || sceneId === 'summary') {       el.objectiveCard?.classList.add('is-finish-mode');     }   }    function renderLauncherHint(modeLabel = 'Adventure') {     renderCoach('🪥', 'พร้อมช่วยฟันแล้ว กดเริ่มได้เลย');     renderObjective(`เริ่มภารกิจ ${modeLabel}`, 'launcher');     renderSceneMood('launcher');   }    function renderSceneMood(sceneId) {     if (!el.sceneStage) return;      const root = el.sceneStage;     root.dataset.scene = sceneId || '';      el.scanCard?.classList.remove('is-emphasis');     el.bossCard?.classList.remove('is-emphasis');     el.helperCard?.classList.remove('is-warning', 'is-success');      document.querySelectorAll('[data-ring-zone]').forEach((ring) => {       ring.classList.remove('is-scene-focus');     });      if (sceneId === 'scan') {       root.style.filter = 'saturate(1.03) brightness(1.02)';       el.scanCard?.classList.add('is-emphasis');       document.querySelectorAll('[data-ring-zone]').forEach((ring) => ring.classList.add('is-scene-focus'));     } else if (sceneId === 'guided') {       root.style.filter = 'brightness(1.01)';       document.querySelectorAll('[data-ring-zone]').forEach((ring, index) => {         if (index < 2) ring.classList.add('is-scene-focus');       });     } else if (sceneId === 'pressure' || sceneId === 'fever') {       root.style.filter = 'saturate(1.06) contrast(1.01)';       el.helperCard?.classList.add('is-warning');       document.querySelectorAll('[data-ring-zone]').forEach((ring, index) => {         if (index % 2 === 0) ring.classList.add('is-scene-focus');       });     } else if (sceneId === 'bossBreak' || sceneId === 'boss') {       root.style.filter = 'saturate(1.10) contrast(1.03)';       el.bossCard?.classList.add('is-emphasis');       el.helperCard?.classList.add('is-warning');       document.querySelectorAll('[data-ring-zone]').forEach((ring) => ring.classList.add('is-scene-focus'));     } else if (sceneId === 'finish' || sceneId === 'summary') {       root.style.filter = 'brightness(1.05) saturate(1.02)';       el.helperCard?.classList.add('is-success');       document.querySelectorAll('[data-ring-zone]').forEach((ring) => ring.classList.add('is-scene-focus'));     } else {       root.style.filter = '';     }   }    return {     renderTopHud,     renderMiniMap,     renderCoach,     renderScanHud,     renderBossBreakHud,     renderSummary,     closeSummary,     renderObjective,     renderLauncherHint,     renderSceneMood   }; }

