import { GAME_ID, GAME_VARIANT, GAME_TITLE, ZONE } from './brush.constants.js';  function clamp(v, a, b) {   return Math.max(a, Math.min(b, v)); }  export function buildPhaseBreakdown(state) {   return Object.entries(state.metrics.phaseStats || {}).map(([phaseId, p]) => {     const attempts = p.attempts || 0;     const valid = p.valid || 0;     return {       phaseId,       durationMs: p.durationMs || 0,       attempts,       valid,       accuracyPercent: attempts ? Math.round((valid / attempts) * 100) : 0,       cleanGain: Math.round(p.cleanGain || 0),       scoreGain: Math.round(p.scoreGain || 0)     };   }); }  export function buildZoneBreakdown(state) {   return (state.zones || []).map((z) => {     const attempts = (z.hits || 0) + (z.misses || 0);     return {       zoneId: z.id,       zoneLabel: z.label,       cleanPercent: Math.round(z.cleanPercent || 0),       threatPercentEnd: Math.round(z.threatPercent || 0),       done: !!z.done,       attempts,       valid: z.hits || 0,       misses: z.misses || 0,       accuracyPercent: attempts ? Math.round(((z.hits || 0) / attempts) * 100) : 0,       dwellMs: z.dwellMs || 0,       patternType: z.patternType || 'horizontal'     };   }); }  export function buildSummaryAdvice(result) {   const tips = [];   if ((result.scanSummary?.accuracyPercent || 0) < 60) tips.push('ลองสังเกตจุดสกปรกให้ชัดก่อนเริ่มแปรง');   if ((result.accuracyPercent || 0) < 60) tips.push('ควรแปรงตามทิศให้ตรงและสม่ำเสมอขึ้น');   if ((result.zoneBalancePercent || 0) < 60) tips.push('ควรกระจายเวลาไปทุกโซนให้สมดุลขึ้น');   if ((result.bossBreakSummary?.played && !result.bossBreakSummary?.success)) tips.push('ฝึกจับจังหวะจุดอ่อนของบอสให้แม่นขึ้น');   return tips.length ? tips.join(' • ') : 'ยอดเยี่ยมมาก วันนี้ช่วยทั้งปากได้ดีมาก'; }  export function buildCsvRow(result) {   return {     sessionId: result.sessionId,     gameId: GAME_ID,     variant: GAME_VARIANT,     pid: result.pid || '',     name: result.name || '',     modeId: result.modeId || '',     diff: result.diff || '',     view: result.view || '',     startedAt: result.startedAt,     endedAt: result.endedAt,     elapsedSec: result.elapsedSec,     finalScore: result.finalScore,     finalRank: result.finalRank,     coveragePercent: result.coveragePercent,     accuracyPercent: result.accuracyPercent,     patternQualityPercent: result.patternQualityPercent,     zoneBalancePercent: result.zoneBalancePercent,     bossRecoveryScore: result.bossRecoveryScore,     scanPlayed: result.scanSummary?.played ? 1 : 0,     scanAccuracyPercent: result.scanSummary?.accuracyPercent || 0,     bossBreakPlayed: result.bossBreakSummary?.played ? 1 : 0,     bossBreakAccuracyPercent: result.bossBreakSummary?.accuracyPercent || 0,     bossBreakSuccess: result.bossBreakSummary?.success ? 1 : 0   }; }  export function buildBrushV5Result({ state, ctx }) {   const hits = state.metrics.hits || 0;   const misses = state.metrics.misses || 0;   const attempts = hits + misses;   const coveragePercent = Math.round((state.zones || []).reduce((a, z) => a + (z.cleanPercent || 0), 0) / Math.max(1, state.zones.length));   const accuracyPercent = attempts ? Math.round((hits / attempts) * 100) : 0;   const zoneCleanValues = (state.zones || []).map(z => z.cleanPercent || 0);   const avg = zoneCleanValues.length ? zoneCleanValues.reduce((a, b) => a + b, 0) / zoneCleanValues.length : 0;   const spread = zoneCleanValues.length ? zoneCleanValues.reduce((a, v) => a + Math.abs(v - avg), 0) / zoneCleanValues.length : 100;   const zoneBalancePercent = clamp(Math.round(100 - spread), 0, 100);   const patternQualityPercent = clamp(Math.round(accuracyPercent * 0.7 + (state.score.comboMax || 0) * 2), 0, 100);   const bossRecoveryScore = clamp(Math.round((state.boss?.cleared ? 70 : 20) + (100 - (state.threat?.percent || 100)) * 0.3), 0, 100);    let finalRank = 'C';   if (coveragePercent >= 90 && accuracyPercent >= 85) finalRank = 'S';   else if (coveragePercent >= 80 && accuracyPercent >= 75) finalRank = 'A';   else if (coveragePercent >= 65 && accuracyPercent >= 60) finalRank = 'B';    const result = {     sessionId: ctx.sessionId,     gameId: GAME_ID,     gameVariant: GAME_VARIANT,     gameTitle: GAME_TITLE,     zone: ZONE,      pid: ctx.pid || '',     name: ctx.name || '',     studyId: ctx.studyId || '',     modeId: ctx.modeId || '',     diff: ctx.diff || '',     view: ctx.view || '',     runMode: ctx.runMode || '',     seed: ctx.seed || '',      startedAt: state.time.startedAtIso,     endedAt: new Date().toISOString(),     elapsedSec: Math.round(state.time.elapsedMs / 1000),      finalScore: Math.round(state.score.total || 0),     finalRank,      coveragePercent,     accuracyPercent,     patternQualityPercent,     zoneBalancePercent,     bossRecoveryScore,      comboMax: Math.floor(state.score.comboMax || 0),     warnings: state.metrics.warnings || 0,     misses,      scanSummary: { ...state.scan },     bossBreakSummary: { ...state.bossBreak },     bossSummary: { ...state.boss },      phaseBreakdown: buildPhaseBreakdown(state),     zoneBreakdown: buildZoneBreakdown(state)   };    result.summaryAdvice = buildSummaryAdvice(result);   result.csvRow = buildCsvRow(result);   return result; }
