import { GAME_ID, GAME_VARIANT } from './brush.constants.js';  const STORAGE_SCHEMA_VERSION = 1; const NS = ['HHA', GAME_ID, GAME_VARIANT, `schema${STORAGE_SCHEMA_VERSION}`].join('__');  function readJson(key, fallback = null) {   try {     return JSON.parse(localStorage.getItem(key) || 'null') ?? fallback;   } catch {     return fallback;   } }  function writeJson(key, value) {   try {     localStorage.setItem(key, JSON.stringify(value));     return true;   } catch {     return false;   } }  export function createBrushStorage() {   const keys = {     lastResult: `${NS}__LAST_RESULT`,     csvRows: `${NS}__CSV_ROWS`   };    return {     keys,     loadLastResult() {       return readJson(keys.lastResult, null);     },     saveLastResult(result) {       return writeJson(keys.lastResult, result);     },     loadCsvRows() {       const rows = readJson(keys.csvRows, []);       return Array.isArray(rows) ? rows : [];     },     appendCsvRow(row) {       const rows = this.loadCsvRows();       rows.push(row);       return writeJson(keys.csvRows, rows);     }   }; }
