/* =========================================================
   EAP Word Quest • Shared Apps Script Project Notice

   This project now uses:
   1) SharedWebAppRouter.gs  → the one and only doGet/doPost
   2) EAPWordQuest.gs        → EAP Word Quest module functions

   This file intentionally contains no executable functions. It remains only
   as a compatibility notice so copying the whole folder into the shared Apps
   Script project cannot create duplicate doGet/doPost declarations.
========================================================= */
