(function(){
'use strict';
const VERSION='2026-08-18-TEACHER-XLSX-V2-MOBILE-OFFICE';
const SHEETJS_URL='https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/xlsx.full.min.js';
const SESSION_IDS=['D1-AM','D1-PM','D2-AM','D2-PM','D3-AM','D3-PM'];
const LIMIT=300,CHUNK=30,BONUS_PASS=80,REWARD_LIMIT=28;
const COL={profiles:'ewp_profiles',progress:'ewp_progress',summary:'ewp_game_summary',checkpoints:'ewp_assessment_checkpoints',assessments:'ewp_assessments',teacherRoles:'ewp_teacher_roles',rewards:'ewp_bonus_rewards'};
const GAMES=[
 {id:'word_match',title:'LexiMatch Navigator',pass:55},
 {id:'category_forest',title:'Category Forest',pass:60},
 {id:'sentence_city',title:'Sentence City',pass:60},
 {id:'word_detective',title:'Conversation Quest',pass:60},
 {id:'final_boss',title:'LEXICON Champion Arena',pass:60}
];
const PARTICIPANT_HEADERS=['Session','Player ID','Name','Stage','Pre %','Post %','Gain (pp)','Game 1 %','Game 2 %','Game 3 %','Game 4 %','Game 5 %','Game Avg %','Reflection','Journey Summary','Certificate','Certificate ID','Finished At (Bangkok)'];
const PARTICIPANT_WIDTHS=[11,16,28,19,10,10,11,10,10,10,10,10,12,12,16,12,20,22];
const $=id=>document.getElementById(id);
const clean=v=>String(v==null?'':v).trim();
const n=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
const ts=v=>{try{return typeof v?.toMillis==='function'?v.toMillis():(v?.seconds?Number(v.seconds)*1000:new Date(v||0).getTime()||0)}catch(_){return 0}};
const chunks=(a,size=CHUNK)=>{const out=[];for(let i=0;i<a.length;i+=size)out.push(a.slice(i,i+size));return out};
const docs=s=>s.docs.map(d=>({id:d.id,...(d.data()||{})}));
function status(text,bad=false){const el=$('apiStatus');if(!el)return;el.textContent=text;el.className='status'+(bad?' bad':'')}
function scoreOf(x){if(!x)return null;for(const k of ['accuracy','accuracyPct','percent','percentage'])if(Number.isFinite(Number(x[k])))return n(x[k]);if(n(x.total)>0&&Number.isFinite(Number(x.score)))return Math.round(n(x.score)/n(x.total)*100);return null}
function gameAverage(scores){const a=GAMES.map(g=>Number(scores?.[g.id])).filter(Number.isFinite);return a.length?Math.round(a.reduce((x,y)=>x+y,0)/a.length):0}
function stageLabel(p,summaryViewed){if(!p.preDone)return'Pre-Challenge';const a=Array.isArray(p.passed)?p.passed:[];if(!a.includes('word_match'))return'Game 1';if(!a.includes('category_forest'))return'Game 2';if(!a.includes('sentence_city'))return'Game 3';if(!a.includes('word_detective'))return'Game 4';if(!a.includes('final_boss'))return'Game 5';if(!p.postDone)return'Post-Challenge';if(!p.reflectionDone&&!p.finalReflection)return'Final Reflection';if(!summaryViewed)return'Journey Summary';return'Complete'}
function bangkokDate(v){const ms=typeof v==='number'?v:ts(v);if(!ms)return'';try{const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Bangkok',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).formatToParts(new Date(ms)),m=Object.fromEntries(parts.map(p=>[p.type,p.value]));return `${m.year}-${m.month}-${m.day} ${m.hour}:${m.minute}:${m.second}`}catch(_){return new Date(ms).toISOString()}}
async function ensureXlsx(){
 if(window.XLSX?.utils?.book_new)return window.XLSX;
 const existing=document.querySelector('script[data-ew-sheetjs]');
 if(existing){await new Promise((resolve,reject)=>{if(window.XLSX)return resolve();existing.addEventListener('load',resolve,{once:true});existing.addEventListener('error',reject,{once:true})});return window.XLSX}
 await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=SHEETJS_URL;s.async=true;s.dataset.ewSheetjs='1';s.onload=resolve;s.onerror=()=>reject(new Error('SHEETJS_LOAD_FAILED'));document.head.appendChild(s)});
 if(!window.XLSX?.utils?.book_new)throw new Error('XLSX_ENGINE_NOT_READY');
 return window.XLSX;
}
async function verifyTeacher(){const user=window.firebase?.auth?.().currentUser;if(!user||user.isAnonymous)throw new Error('TEACHER_SIGN_IN_REQUIRED');const snap=await firebase.firestore().collection(COL.teacherRoles).doc(user.uid).get();const role=snap.exists?snap.data():null;if(!role||role.active!==true||role.role!=='teacher')throw new Error('TEACHER_ROLE_REQUIRED');return user}
async function readByIds(col,ids){if(!ids.length)return[];const db=firebase.firestore(),fp=firebase.firestore.FieldPath.documentId();const snaps=await Promise.all(chunks([...new Set(ids)]).map(part=>db.collection(col).where(fp,'in',part).get()));return snaps.flatMap(docs)}
async function selectiveCheckpoints(progress,summaryMap){const keys=[];progress.forEach(p=>{const id=clean(p.playerId||p.id),s=summaryMap.get(id)||{};if(p.preDone&&!Number.isFinite(Number(s.preAccuracy)))keys.push(`${id}__pre`);if(p.postDone&&!Number.isFinite(Number(s.postAccuracy)))keys.push(`${id}__post`)});return readByIds(COL.checkpoints,keys)}
async function selectiveAssessments(progress,summaryMap,checkpointMap){const ids=[];progress.forEach(p=>{const id=clean(p.playerId||p.id),s=summaryMap.get(id)||{},preCp=checkpointMap.get(`${id}__pre`),postCp=checkpointMap.get(`${id}__post`);const needPre=Boolean(p.preDone)&&!Number.isFinite(Number(s.preAccuracy))&&scoreOf(preCp)==null,needPost=Boolean(p.postDone)&&!Number.isFinite(Number(s.postAccuracy))&&scoreOf(postCp)==null;if(needPre||needPost)ids.push(id)});const unique=[...new Set(ids)].filter(Boolean);if(!unique.length)return[];const db=firebase.firestore(),snaps=await Promise.all(chunks(unique).map(part=>db.collection(COL.assessments).where('playerId','in',part).get()));return snaps.flatMap(docs)}
function latestAssessmentMap(assessments){const map=new Map();assessments.forEach(a=>{const id=clean(a.playerId),type=clean(a.assessmentType||a.type).toLowerCase();if(!id||!['pre','post'].includes(type))return;const key=`${id}__${type}`,prev=map.get(key),at=ts(a.submittedAt||a.updatedAt||a.createdAt),prevAt=prev?ts(prev.submittedAt||prev.updatedAt||prev.createdAt):-1;if(!prev||at>=prevAt)map.set(key,a)});return map}
async function loadSessionData(sessionId){
 const db=firebase.firestore();
 const progress=docs(await db.collection(COL.progress).where('attendanceSessionId','==',sessionId).limit(LIMIT).get()),ids=progress.map(p=>clean(p.playerId||p.id)).filter(Boolean);
 const [profiles,summaries,rewards]=await Promise.all([readByIds(COL.profiles,ids),readByIds(COL.summary,ids),db.collection(COL.rewards).where('sessionId','==',sessionId).limit(LIMIT).get().then(docs)]);
 const pm=new Map(profiles.map(x=>[clean(x.playerId||x.id),x])),sm=new Map(summaries.map(x=>[clean(x.playerId||x.id),x]));
 const cps=await selectiveCheckpoints(progress,sm),cm=new Map(cps.map(x=>[x.id,x])),assessments=await selectiveAssessments(progress,sm,cm),am=latestAssessmentMap(assessments);
 const rows=progress.map(p=>{const id=clean(p.playerId||p.id),profile=pm.get(id)||{},s=sm.get(id)||{},preCp=cm.get(`${id}__pre`),postCp=cm.get(`${id}__post`),preAss=am.get(`${id}__pre`),postAss=am.get(`${id}__post`);const pre=Number.isFinite(Number(s.preAccuracy))?n(s.preAccuracy):(scoreOf(preCp)!=null?scoreOf(preCp):scoreOf(preAss)),post=Number.isFinite(Number(s.postAccuracy))?n(s.postAccuracy):(scoreOf(postCp)!=null?scoreOf(postCp):scoreOf(postAss)),cert=Boolean(p.certificateEligible||p.certificate?.certificateId),summaryViewed=Boolean(p.summaryViewed||cert),scores=s.bestScores&&typeof s.bestScores==='object'?s.bestScores:(p.bestScores||{});return {playerId:id,nickname:clean(profile.nickname||profile.fullName||p.nickname||id),fullName:clean(profile.fullName||profile.nickname||p.nickname||id),sessionId,stage:stageLabel(p,summaryViewed),preDone:Boolean(p.preDone||pre!=null),postDone:Boolean(p.postDone||post!=null),preAccuracy:pre,postAccuracy:post,learningGain:pre!=null&&post!=null?post-pre:null,bestScores:scores,avg:gameAverage(scores),reflectionDone:Boolean(p.reflectionDone||p.finalReflection),summaryViewed,certificateEligible:cert,certificateId:clean(p.certificate?.certificateId),finishedAt:p.finishedAt}}).sort((a,b)=>a.fullName.localeCompare(b.fullName));
 return {sessionId,rows,rewardRows:rewards.filter(r=>r.completed===true&&n(r.bonusScore,-1)>=BONUS_PASS)};
}
function eligibleRewards(bundle){const finish=new Map(bundle.rows.filter(r=>r.summaryViewed&&r.certificateEligible&&ts(r.finishedAt)>0).map(r=>[r.playerId,r]));return bundle.rewardRows.filter(r=>finish.has(clean(r.playerId))&&ts(r.firstCompletedAt)>0&&n(r.bonusScore,-1)>=BONUS_PASS).map(r=>{const f=finish.get(clean(r.playerId));return {...r,finish:f,qualifiedAtMs:Math.max(ts(f.finishedAt),ts(r.firstCompletedAt))}}).sort((a,b)=>a.qualifiedAtMs-b.qualifiedAtMs||clean(a.playerId).localeCompare(clean(b.playerId))).slice(0,REWARD_LIMIT)}
function overview(rows){const total=rows.length,pre=rows.filter(r=>r.preDone).length,post=rows.filter(r=>r.postDone).length,refl=rows.filter(r=>r.reflectionDone).length,cert=rows.filter(r=>r.certificateEligible).length,paired=rows.filter(r=>r.preAccuracy!=null&&r.postAccuracy!=null),mean=a=>a.length?Math.round(a.reduce((x,y)=>x+y,0)/a.length):null;return {total,pre,post,refl,cert,pairedN:paired.length,meanPre:mean(paired.map(r=>r.preAccuracy)),meanPost:mean(paired.map(r=>r.postAccuracy)),gain:mean(paired.map(r=>r.learningGain))}}
function participantData(rows){return rows.map(r=>[r.sessionId,String(r.playerId),r.fullName,r.stage,r.preAccuracy==null?'':Math.round(r.preAccuracy),r.postAccuracy==null?'':Math.round(r.postAccuracy),r.learningGain==null?'':Math.round(r.learningGain),...GAMES.map(g=>Number.isFinite(Number(r.bestScores?.[g.id]))?Math.round(n(r.bestScores[g.id])):''),Math.round(r.avg),r.reflectionDone?'Yes':'No',r.summaryViewed?'Yes':'No',r.certificateEligible?'Yes':'No',r.certificateId,bangkokDate(r.finishedAt)])}
function addSheet(wb,name,headers,data,widths){const aoa=[headers,...data],ws=XLSX.utils.aoa_to_sheet(aoa);ws['!cols']=(widths||headers.map(()=>14)).map(wch=>({wch}));ws['!autofilter']={ref:`A1:${XLSX.utils.encode_col(headers.length-1)}${Math.max(1,aoa.length)}`};XLSX.utils.book_append_sheet(wb,ws,name)}
function writeWorkbook(wb,filename){wb.Props={Title:'LEXICON X Teacher Export',Subject:'English Week Passport learning analytics',Author:'LEXICON X Teacher Console',CreatedDate:new Date()};XLSX.writeFile(wb,filename,{bookType:'xlsx',compression:true})}
function currentFilters(rows){const q=clean($('searchInput')?.value).toLowerCase(),stage=clean($('stageFilter')?.value);return rows.filter(r=>(!q||`${r.playerId} ${r.fullName}`.toLowerCase().includes(q))&&(!stage||r.stage===stage))}
function buildCurrentWorkbook(bundle,list){const wb=XLSX.utils.book_new(),o=overview(bundle.rows),winners=eligibleRewards(bundle),gameOverview=GAMES.map((g,i)=>{const p=bundle.rows.filter(r=>Number.isFinite(Number(r.bestScores?.[g.id]))),avg=p.length?Math.round(p.reduce((s,r)=>s+n(r.bestScores[g.id]),0)/p.length):0,pass=p.filter(r=>n(r.bestScores[g.id])>=g.pass).length;return [`Game ${i+1}: ${g.title}`,p.length,avg,pass,g.pass]});addSheet(wb,'Participants',PARTICIPANT_HEADERS,participantData(list),PARTICIPANT_WIDTHS);addSheet(wb,'Overview',['Metric','Value','Detail 1','Detail 2','Detail 3'],[['Exported At (Bangkok)',bangkokDate(Date.now()),'','',''],['Session',bundle.sessionId,'','',''],['Exported Participants',list.length,'','',''],['Session Participants',o.total,'','',''],['Pre Complete',o.pre,'','',''],['Post Complete',o.post,'','',''],['Reflections',o.refl,'','',''],['Certificates',o.cert,'','',''],['Paired N',o.pairedN,'','',''],['Pre Mean %',o.meanPre??'','','',''],['Post Mean %',o.meanPost??'','','',''],['Mean Gain (pp)',o.gain??'','','',''],['Reward Eligible',winners.length,'','',''],['Reward Claimed',winners.filter(r=>r.rewardClaimed===true).length,'','',''],['Game Analytics','','','',''],...gameOverview],[28,16,18,18,20]);addSheet(wb,'Rewards',['Rank','Player ID','Name','Bonus %','Qualified At (Bangkok)','Reward Status'],winners.map((r,i)=>[i+1,String(r.playerId),r.finish?.fullName||r.finish?.nickname||String(r.playerId),n(r.bonusScore),bangkokDate(r.qualifiedAtMs),r.rewardClaimed===true?'Claimed':'Eligible']),[8,16,28,12,22,16]);return wb}
function buildAllWorkbook(bundles){const wb=XLSX.utils.book_new(),allRows=bundles.flatMap(b=>b.rows),allRewards=bundles.flatMap(b=>eligibleRewards(b).map((r,i)=>({...r,sessionId:b.sessionId,rank:i+1}))),summaryRows=bundles.map(b=>{const o=overview(b.rows),eligible=eligibleRewards(b);return [b.sessionId,o.total,o.pre,o.post,o.refl,o.cert,o.pairedN,o.meanPre??'',o.meanPost??'',o.gain??'',eligible.length,eligible.filter(r=>r.rewardClaimed===true).length]}),all=overview(allRows);summaryRows.push(['ALL',all.total,all.pre,all.post,all.refl,all.cert,all.pairedN,all.meanPre??'',all.meanPost??'',all.gain??'',allRewards.length,allRewards.filter(r=>r.rewardClaimed===true).length]);addSheet(wb,'Round Summary',['Session','Participants','Pre Complete','Post Complete','Reflections','Certificates','Paired N','Pre Mean %','Post Mean %','Mean Gain (pp)','Reward Eligible','Reward Claimed'],summaryRows,[11,12,13,13,12,12,10,12,12,14,14,14]);addSheet(wb,'All Participants',PARTICIPANT_HEADERS,participantData(allRows),PARTICIPANT_WIDTHS);addSheet(wb,'Rewards All',['Session','Rank','Player ID','Name','Bonus %','Qualified At (Bangkok)','Reward Status'],allRewards.map(r=>[r.sessionId,r.rank,String(r.playerId),r.finish?.fullName||r.finish?.nickname||String(r.playerId),n(r.bonusScore),bangkokDate(r.qualifiedAtMs),r.rewardClaimed===true?'Claimed':'Eligible']),[11,8,16,28,12,22,16]);bundles.forEach(b=>addSheet(wb,b.sessionId,PARTICIPANT_HEADERS,participantData(b.rows),PARTICIPANT_WIDTHS));return wb}
async function exportCurrent(){const btn=$('exportXlsBtn');try{if(btn)btn.disabled=true;await verifyTeacher();status('XLSX • กำลังเตรียม Excel engine…');await ensureXlsx();const sid=clean($('sessionFilter')?.value)||'D1-AM';status(`XLSX • กำลังอ่าน ${sid}…`);const bundle=await loadSessionData(sid),list=currentFilters(bundle.rows);if(!list.length)throw new Error('NO_DATA_TO_EXPORT');const stamp=new Date().toISOString().slice(0,10).replaceAll('-','');writeWorkbook(buildCurrentWorkbook(bundle,list),`LEXICON_X_${sid}_${stamp}.xlsx`);status(`XLSX exported ✓ • ${sid} • ${list.length} players`)}catch(e){console.error(e);status(`XLSX export failed • ${clean(e.message||e)}`,true)}finally{if(btn)btn.disabled=false}}
async function exportAll(){const btn=$('exportAllXlsBtn'),single=$('exportXlsBtn'),refresh=$('refreshBtn');try{if(btn)btn.disabled=true;if(single)single.disabled=true;if(refresh)refresh.disabled=true;await verifyTeacher();status('XLSX • กำลังเตรียม Excel engine…');await ensureXlsx();const bundles=[];for(let i=0;i<SESSION_IDS.length;i++){status(`XLSX All • กำลังอ่าน ${SESSION_IDS[i]} (${i+1}/6)…`);bundles.push(await loadSessionData(SESSION_IDS[i]))}const total=bundles.reduce((s,b)=>s+b.rows.length,0);if(!total)throw new Error('NO_PARTICIPANT_DATA');const stamp=new Date().toISOString().slice(0,10).replaceAll('-','');writeWorkbook(buildAllWorkbook(bundles),`LEXICON_X_ALL_6_ROUNDS_${stamp}.xlsx`);status(`XLSX exported ✓ • All 6 Rounds • ${total} records`)}catch(e){console.error(e);status(`XLSX All failed • ${clean(e.message||e)}`,true)}finally{if(btn)btn.disabled=false;if(single)single.disabled=false;if(refresh)refresh.disabled=false}}
function replaceButton(id,label,handler){const old=$(id);if(!old)return null;const btn=old.cloneNode(true);btn.textContent=label;btn.disabled=false;old.replaceWith(btn);btn.addEventListener('click',handler);return btn}
function bind(){replaceButton('exportXlsBtn','⬇ Export XLSX',exportCurrent);replaceButton('exportAllXlsBtn','⬇ All 6 Rounds XLSX',exportAll);const brand=document.querySelector('.brand small');if(brand)brand.textContent=brand.textContent.replace('XLS Export','XLSX Export');}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
window.EW_TEACHER_XLSX_V2=Object.freeze({version:VERSION,exportCurrent,exportAll,sheetJs:SHEETJS_URL});
})();
