(function(){"use strict";
const style=document.createElement('style');style.textContent=`.card.target{border-color:#2b536b!important;box-shadow:none!important}.card.target:not(.flipped):not(.matched)::before{display:none!important}#selectionGlider{position:absolute;left:0;top:0;z-index:12;pointer-events:none;border:3px solid #ffe36e;border-radius:13px;box-shadow:0 0 0 3px rgba(255,227,110,.22),0 0 22px rgba(255,227,110,.30);will-change:transform,width,height;transform:translate3d(0,0,0)}#selectionGlider::before{content:'SELECTED';position:absolute;top:4px;left:50%;transform:translateX(-50%);padding:2px 6px;border-radius:999px;background:#ffe36e;color:#12293c;font-size:.48rem;font-weight:950;letter-spacing:.06em;white-space:nowrap}#selectionGlider.moving::before{content:'MOVING';background:#70e7ff}#selectionGlider.skipping::before{content:'SKIPPING OPEN';background:#70e7ff}#selectionGlider.opening-now{border-color:#70e7ff;box-shadow:0 0 0 3px rgba(112,231,255,.22),0 0 24px rgba(112,231,255,.34)}`;document.head.appendChild(style);
const MOVE=360,SKIP=90,SETTLE=350,OPEN=900;let raf=0,motion=null,dwell=0,canOpen=false,swipe=null;
const api=()=>window.LEXICON_V7,card=i=>document.querySelector(`.card[data-index="${i}"]`),glider=()=>document.getElementById('selectionGlider');
function box(i){const e=card(i);return e?{x:e.offsetLeft,y:e.offsetTop,w:e.offsetWidth,h:e.offsetHeight}:null;}
function draw(b){const g=glider();if(!g||!b)return;g.style.width=b.w+'px';g.style.height=b.h+'px';g.style.transform=`translate3d(${b.x}px,${b.y}px,0)`;}
function clearOpen(){canOpen=false;dwell=0;document.querySelectorAll('.card.opening').forEach(e=>{e.classList.remove('opening');e.style.removeProperty('--p');delete e.dataset.pct;});glider()?.classList.remove('opening-now');}
function next(from,dir){const d=api().S.deck,cols=3,rows=Math.ceil(d.length/cols);let r=Math.floor(from/cols),c=from%cols;const dr=dir==='up'?-1:dir==='down'?1:0,dc=dir==='left'?-1:dir==='right'?1:0;let skipped=0;while(true){r+=dr;c+=dc;if(r<0||r>=rows||c<0||c>=cols)return{index:-1,skipped};const i=r*cols+c;if(i>=d.length)return{index:-1,skipped};const x=d[i];if(x&&!x.flipped&&!x.matched)return{index:i,skipped};skipped++;}}
function move(dir){const a=api(),s=a.S;if(!document.getElementById('grid')||s.locked||motion)return;clearOpen();const f=next(s.current,dir);if(f.index<0){a.instruction('ทิศนี้ไม่มีการ์ดที่ยังปิดอยู่');return;}const from=box(s.current),to=box(f.index);if(!from||!to)return;motion={to:f.index,from,to,start:performance.now(),duration:MOVE+Math.min(220,f.skipped*SKIP),skipped:f.skipped};const g=glider();g?.classList.add('moving');if(f.skipped)g?.classList.add('skipping');a.instruction(f.skipped?`ข้ามการ์ดที่เปิดแล้ว ${f.skipped} ใบ`:'กำลังเลื่อนไปยังการ์ดถัดไป');}
function onUp(e){if(!swipe)return;const dx=e.clientX-swipe.x,dy=e.clientY-swipe.y,dt=performance.now()-swipe.t;swipe=null;if(dt>1000||Math.hypot(dx,dy)<26)return;if(Math.abs(dx)>Math.abs(dy))move(dx>0?'right':'left');else move(dy>0?'down':'up');}
function loop(now){const a=api();if(!a||!document.getElementById('grid')){raf=requestAnimationFrame(loop);return;}const s=a.S;if(motion){const t=Math.min(1,(now-motion.start)/motion.duration),e=t<.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2;draw({x:motion.from.x+(motion.to.x-motion.from.x)*e,y:motion.from.y+(motion.to.y-motion.from.y)*e,w:motion.from.w+(motion.to.w-motion.from.w)*e,h:motion.from.h+(motion.to.h-motion.from.h)*e});if(t>=1){document.querySelectorAll('.card.target').forEach(x=>x.classList.remove('target'));s.current=motion.to;card(s.current)?.classList.add('target');motion=null;glider()?.classList.remove('moving','skipping');const c=s.deck[s.current];canOpen=!!(c&&!c.flipped&&!c.matched);dwell=canOpen?now+SETTLE:0;a.instruction('ถึงการ์ดแล้ว • หยุดยืนยันก่อนเปิด');}}
else if(canOpen&&!s.locked){const c=s.deck[s.current],el=card(s.current);if(c&&el&&!c.flipped&&!c.matched&&now>=dwell){const p=Math.min(100,(now-dwell)/OPEN*100);el.classList.add('opening');el.dataset.pct=String(Math.round(p));el.style.setProperty('--p',p+'%');glider()?.classList.add('opening-now');a.instruction('กำลังเปิด '+Math.round(p)+'%');if(p>=100){clearOpen();a.flip(s.current);}}}raf=requestAnimationFrame(loop);}
function setup(){const grid=document.getElementById('grid');if(!grid)return;grid.style.touchAction='none';grid.addEventListener('pointerdown',e=>{clearOpen();swipe={x:e.clientX,y:e.clientY,t:performance.now()};},{passive:true});grid.addEventListener('pointerup',onUp,{passive:true});grid.addEventListener('pointercancel',()=>swipe=null,{passive:true});requestAnimationFrame(()=>draw(box(api().S.current)));}
window.addEventListener('lexicon-v7-ready',setup);window.addEventListener('lexicon-v7-pair-resolved',clearOpen);raf=requestAnimationFrame(loop);window.LEXICON_V7_NAV={version:'2026-08-05-V7-STATIC-NAV',moveMs:MOVE,skipStepMs:SKIP,settleMs:SETTLE,openingMs:OPEN};
}());