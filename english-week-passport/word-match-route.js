(function () {
  "use strict";
  const GAME_URL = "./lexicon-x-challenge.html?v=20260804-lexiconx1";
  const selector = '.stage-card[data-stage="word_match"].clickable';
  function decorateCard(){const card=document.querySelector('.stage-card[data-stage="word_match"]');if(!card||card.dataset.memoryDecorated==="1")return;card.dataset.memoryDecorated="1";card.classList.add("memory-stage-card");const detail=card.querySelector("small");if(detail)detail.textContent="LEXICON X Challenge • Smooth Magnetic Tilt • CEFR A2–B1+";const state=card.querySelector(".stage-state");if(state&&card.classList.contains("ready"))state.innerHTML="LEXICON X พร้อมเล่น";}
  function openGame(event){const card=event.target?.closest?.(selector);if(!card)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();location.href=GAME_URL;}
  function openByKeyboard(event){if(event.key!=="Enter"&&event.key!==" ")return;openGame(event);}
  function autoResume(){const params=new URLSearchParams(location.search);if(params.get("resume")!=="memory")return;let identity=null;try{identity=JSON.parse(localStorage.getItem(window.EW_CONFIG.cacheKeys.identity)||"null");}catch(_){}if(!identity?.playerId)return;const form=document.getElementById("loginForm"),input=document.getElementById("playerId");if(!form||!input)return;input.value=identity.playerId;const nick=document.getElementById("nickname");if(nick)nick.value=identity.nickname||identity.fullName||"";params.delete("resume");history.replaceState(null,"",`${location.pathname}${params.toString()?`?${params}`:""}`);if(typeof form.requestSubmit==="function")form.requestSubmit();else form.dispatchEvent(new Event("submit",{bubbles:true,cancelable:true}));}
  const style=document.createElement("style");style.textContent=`.stage-card.memory-stage-card.ready{border-color:#38bdf8;background:linear-gradient(135deg,#f8fbff,#e8f3fb)}.stage-card.memory-stage-card .stage-icon{background:linear-gradient(135deg,#e9f3fa,#dcecf8);position:relative}.stage-card.passed.memory-stage-card .stage-icon::after{content:" LX";font-size:.42rem;font-weight:900;color:#184d73;position:absolute;margin-top:48px}`;document.head.appendChild(style);
  document.addEventListener("click",openGame,true);document.addEventListener("keydown",openByKeyboard,true);new MutationObserver(decorateCard).observe(document.getElementById("screen")||document.body,{childList:true,subtree:true});decorateCard();setTimeout(autoResume,70);
}());
