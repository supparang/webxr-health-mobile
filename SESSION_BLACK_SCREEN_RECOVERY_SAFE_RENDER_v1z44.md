# SESSION_BLACK_SCREEN_RECOVERY_SAFE_RENDER_v1z44

Fixes:
- Some sessions may open to a black screen due to runtime render errors.

Adds:
- safeOpenSession()
- renderSessionRecoveryPanel()
- try/catch around renderSkillPath()
- safer evidence badge DOM augmentation
- global black screen guard
